#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char low;

for (low = 'a'; low <= 'z'; low++)
  printf("%c",low);
for (low = 'A'; low <= 'Z'; low++)
  printf("5c",low);
 printf("\n");

return (0);
}
